<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="my_dialog" parent="@android:Theme.Dialog">
        <item name="android:windowFrame">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowBackground">@color/alpha</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowContentOverlay">@null</item>
    </style>

    <!-- 公用listView -->
    <style name="listViewTheme">

        <!--
        <item name="android:divider">@color/line</item>
        <item name="android:dividerHeight">@dimen/line_thin</item>

        -->
        <item name="android:cacheColorHint">@color/alpha</item>
        <!-- <item name="android:listSelector">@color/alpha</item> -->
        <item name="android:scrollbars">none</item>
    </style>

</resources>
